<Page x:Class="pract15_trpo.Pages.CategoryList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:pract15_trpo.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Список категорий">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button HorizontalAlignment="Left" VerticalAlignment="Top" Margin="5" Click="GoBack">Назад</Button>
        <StackPanel x:Name="Form" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Width="250">
            <Button Margin="5" Click="GoForm">Добавить новый</Button>
            <Button Margin="5" Click="Remove">Удалить выбранный</Button>
        </StackPanel>
        <ListView Grid.Column="1"
                  x:Name="ListViewCategory"
                  HorizontalContentAlignment="Stretch"
                  Margin="5"
                  ItemsSource="{Binding CategoryService.Categories}"
                  SelectedItem="{Binding selected}"
                  MouseDoubleClick="GoEdit">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1">
                        <StackPanel>
                            <TextBlock>
                                <Run Text="ID: "/>
                                <Run Text="{Binding Id}"/>
                            </TextBlock>
                            <TextBlock>
                                <Run Text="Категория: "/>
                                <Run Text="{Binding Name}"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
